# for docker build
VERSION=${VERSION:-"22.12-py3"}
VARIANT=${VARIANT:-"a1111"}
USERNAME=${USERNAME:-"$(whoami)"}
DOCKER_USERNAME=${DOCKER_USERNAME:-"$(whoami)"}
USER_UID=${USER_UID:-"$(id -u)"}
USER_GID=${USER_GID:-"$(id -g)"}

IMAGE_TAG="${VERSION}-${VARIANT}"
IMAGE_NAME="${DOCKER_USERNAME}/devenv:${IMAGE_TAG}"
BUILD_FROM="nvcr.io/nvidia/pytorch:${VERSION}"

USER_FULLNAME=${USER_FULLNAME:-""}
USER_EMAIL=${USER_EMAIL:-""}
GITHUB_USERNAME=${GITHUB_USERNAME:-""}
CONTAINER_WORKSPACE_LOCATION="/"
CONTAINER_WORKSPACE_ROOT="/workspace"
CONTAINER_HOSTNAME="devenv-${VARIANT}-${SYSTEM_HOSTNAME}"

# for docker run
# CONTAINER_NAME="ekorpkit-dev-${USERNAME}"
DOCKER_NETWORK_NAME="devenv-network"
CUDA_DEVICE_ID="all"
HOST_SSH_PORT="2132"

# development
HOST=${HOST:-"$(hostname)"}
HOST_JUPYTER_PORT="18685"
JUPYTER_TOKEN="$(passage JUPYTER_TOKEN)"
JUPYTER_PORT="8585"
HOST_GRADIO_PORT="17860"
GRADIO_PORT="7860"

# for NGROK
# HOST_JUPYTER_IP="$(passage devenv/${HOST}/HOST_JUPYTER_IP)"
# NGROK_AUTH="$(passage devenv/${HOST}/NGROK_AUTH)"
# NGROK_PORT="${HOST_JUPYTER_IP}:${HOST_JUPYTER_PORT}"
# NGROK_PROTOCOL="http"
